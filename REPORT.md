# Ripe Strawberry Detector – Report

## Introduction
- Problem: Detect and count ripe strawberries in images for agriculture workflows.
- Objectives: Compare a classical OpenCV baseline vs a fine-tuned YOLOv11 detector and deliver an interactive UI.
- Scope: Image detection only. Video/audio out of scope for this version.

## Research Background
- Prior work summary (insert 3–6 peer‑reviewed sources):
  - Object detection in agriculture (fruit detection/quality grading).
  - YOLO variants performance on small objects/occlusions.
  - Classical vision baselines vs deep learning tradeoffs.
- Critical comparison: dataset needs, robustness to lighting/scale, compute cost.

## Methodology
- Data: Kaggle “Ripe Strawberries Detection Dataset”.
- Pipeline overview:
  - `src/yolo_utils.py`: Parse XML, split train/val, write YOLO labels and `data.yaml` (absolute root path).
  - `src/baseline.py`: Template matching + NMS for classical baseline.
  - `src/train_and_infer.py`: Train YOLOv11, run `val`, and run inference with counting.
  - `streamlit_ui.py`: Interactive image demo for inference/visualization.
- Model/training config (from `yolo11s_demo.ipynb`):
  - Base weights: `yolo11s.pt`
  - Image size: 640; epochs: as per notebook; early stopping `patience`=50.
  - Seeded runs when available; GPU if present.
- Hardware: Specify your machine (CPU/GPU, RAM, CUDA version).
- System design diagram: insert `docs/system_design.png` (optional).

## Results
- Dataset split: Val = 8 images (per Ultralytics val cache). Train split as per notebook.
- Validation metrics (YOLOv11 finetuned):
  ```json
  {
    "metrics/precision(B)": 0.9466030779659758,
    "metrics/recall(B)": 0.7341772151898734,
    "metrics/mAP50(B)": 0.8842871265592139,
    "metrics/mAP50-95(B)": 0.5794882785671164,
    "fitness": 0.6099681633663261
  }
  ```
- Inference performance (validation): ~14.1 ms/image (Ultralytics report).
- Counting accuracy on sample validation images:

  | filename | gt_count | yolo_count | diff |
  |---|---:|---:|---:|
  | 1.png | 24 | 18 | -6 |
  | 13.png | 9 | 10 | 1 |
  | 14.png | 12 | 11 | -1 |
  | 15.png | 7 | 6 | -1 |
  | 16.png | 9 | 9 | 0 |
  | 21.png | 9 | 9 | 0 |
  | 22.png | 4 | 4 | 0 |
  | 24.png | 5 | 7 | 2 |
- Error analysis: Under-count on dense/occluded clusters (e.g., 1.png); small objects at distance; occasional over-count from near-duplicates.

## Discussion and Conclusion
- Did the system meet objectives?
- Why YOLO outperforms baseline (robustness to scale/lighting). When baseline works well.
- Limitations: small objects, domain shift, labeling quality.
- Future work: dataset expansion, augmentations, model size comparison, pruning/quantization, deployment.

## References
- Use consistent citation format (conference/journal). Example placeholders:
  - Author et al., Year, Title, Venue.
  - Author et al., Year, Title, Venue.

## Appendix (optional)
- Exact commands/steps to reproduce:
  - Run `yolo11s_demo.ipynb` end‑to‑end to prepare data, train, and validate.
  - Launch UI: `streamlit run streamlit_ui.py`.
- Notes: The generic pretrained model does not include a "strawberry" class; counts are only valid with finetuned weights.

## Before vs After (autogenerated)
- Run `before_after_metrics.ipynb` to regenerate. Full tables/metrics are saved to `output/report_metrics.md`.
